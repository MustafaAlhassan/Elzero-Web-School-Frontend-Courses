/*
    beforeAll(fn, timeout), default of timeout here is 5s
    beforeEach(fn, timeout)
*/

beforeAll(() => {
    // connect To Database
    // Empty Testing Table
    // Add Dummy Data For Testing
    // Prepare Everything Before Testing
})

afterAll(() => {
    // Clean Database
    // Clean Cache
})


